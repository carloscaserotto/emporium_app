<%=@author.errors.full_messages if @author.errors.any?%>

<%= form_with url: author_index_path , method: :post do |form| %>
  <%= render 'form' %>
  <%= form.submit "Create" %>
<% end %>

<%= link_to 'Back', :action => 'index' %>